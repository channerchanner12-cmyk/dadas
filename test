// Super Wide and Giant Map for Sandboxels

// ==========================================
// 1. Redefine o tamanho do mapa (Ação principal)
// ==========================================
// Aumenta a largura (width) e altura (height) para criar um mapa gigantesco.
// O valor padrão costuma ser 150x100. Aqui, definimos como 500x300.
// Sinta-se à vontade para aumentar ainda mais esses números!

width = 1500;  // Largura do mapa em pixels
height = 900; // Altura do mapa em pixels

// ==========================================
// 2. Ajuste de escala de renderização (Opcional)
// ==========================================
// Diminuir o tamanho do pixel (pixelSize) permite que mais detalhes caibam na tela.
// O valor padrão é 4. Valores menores como 2 ou 1 tornam a visão mais "zoom out".

pixelSize = 3; // Ajuste para 2 ou 1 se quiser ver ainda mais do mapa de uma vez.

// ==========================================
// 3. Ajuste do tamanho da interface (Opcional)
// ==========================================
// Isso ajuda a evitar que a barra de ferramentas ocupe muito espaço.
// Aumentar a largura da tela ou diminuir a escala pode ajudar a acomodar melhor o mapa gigante.

// screenWidth = 1920;  // Descomente e ajuste se necessário
// uiScale = 0.8;      // Descomente e ajuste se necessário

console.log("🚀 Mod de Mapa Gigante Ativado! Largura: " + width + ", Altura: " + height);
